<div>
{%
  liquid
  assign x = 1
  assign y = 2
  if A
    assign z = 1
  endif
%}
</div>
<div>
{%- liquid
  assign x = 1
  assign y = 2
  if A
    assign z = 1
  endif -%}
</div>

It should paste as is
{%- liquid
  if empty  and true
    render 'icon-cart-empty'
  else
    render 'icon-cart'
  endif
-%}
